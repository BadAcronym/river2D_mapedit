const char header[8] = "r2Dtiles";
uint16_t  tilesize;
uint32_t  map_width;
uint32_t  map_height;
uint8_t   layers;

**tilesheet**
(size tilesheet_width * tilesheet_height * RIVER2D_BPP)

**array of tile metadata**
(size tilesheet_width / tilesize * tilesheet_height / tilesize * sizeof(TileMetadata))

**array of placed tile indices**
(size layers * map_width * map_height * sizeof(TileIndex))
